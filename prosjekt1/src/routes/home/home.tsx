import styles from './home.module.css';

const Home = () => (
    <div className={styles.homeContainer}>
        <main>
            <h1>Hello World</h1>
        </main>
    </div>
);

export default Home;
